# User Preferences

Layer 1 of the 5-layer memory system. This file accumulates during use — the Memory Agent appends to it whenever the user expresses a preference, either explicitly ("always use X") or implicitly (consistent pattern across tasks).

Do not reset or reformat this file. Append new observations as sub-bullets or new items under the relevant section.

---

## Coding Style

<!-- Formatting preferences, naming conventions, comment density, language idioms.
     Example entries (added by Memory Agent):
     - Prefers type annotations on all function signatures.
     - Prefers short variable names in tight loops; descriptive names elsewhere.
-->

## Workflow Preferences

<!-- How the user likes task decomposition, retry behavior, verbosity of reports.
     Example entries:
     - Prefers Planner to ask one clarifying question before decomposing, not after.
     - Prefers concise final reports (outcome + changed files only, no verbose rationale).
-->

## Domain-Specific Preferences

<!-- Preferences tied to the target domain (MindSpore/Ascend/MindFormers).
     Example entries:
     - Prefers Ascend 910B as the target device when device is unspecified.
     - Prefers MindFormers Trainer API over raw MindSpore training loops.
-->
